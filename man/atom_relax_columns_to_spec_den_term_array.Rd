% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ke.R
\name{atom_relax_columns_to_spec_den_term_array}
\alias{atom_relax_columns_to_spec_den_term_array}
\title{Convert one rate block of atom-relax columns into a spectral-density term array}
\usage{
atom_relax_columns_to_spec_den_term_array(atom_relax_df, rate_name)
}
\arguments{
\item{atom_relax_df}{data frame containing one or more rate blocks encoded as
atom-relax columns}

\item{rate_name}{character scalar giving the rate block to extract}
}
\value{
A list with elements:
\describe{
\item{\code{value}}{numeric vector from the \code{"<rate_name>_value"} column, or
\code{NULL} if that column is absent}
\item{\code{k}}{numeric vector from the \code{"<rate_name>_k"} column, or \code{NULL}
if that column is absent}
\item{\code{spec_den_term_array}}{numeric array \verb{(pairs, terms, 2)} with
second-dimension names taken from \verb{<freq_name>} and third-dimension
names \code{c("coef", "freq")}}
}
}
\description{
Reconstruct a per-rate spectral-density term array from columns in an
\verb{*_atom_relax.csv}-style data frame.
}
\details{
The expected column naming convention is
\code{"<rate_name>_<freq_name>_<component>"}, where \verb{<component>} is either
\code{"coef"} or \code{"freq"}. If present, the special columns
\code{"<rate_name>_value"} and \code{"<rate_name>_k"} store the target relaxation
values and optional loss weights associated with that rate block.

Parsing is anchored to the supplied \code{rate_name}, so the rate name itself may
contain underscores. The \code{freq_name} portion must not contain underscores.
For each \code{freq_name}, both \code{coef} and \code{freq} columns must be present.
}
\examples{
atom_relax_df <- data.frame(
  r1_value = c(1.0, 2.0),
  r1_k = c(4.0, 9.0),
  r1_0_coef = c(0.1, 0.2),
  r1_0_freq = c(0, 0),
  r1_2wH_coef = c(0.3, 0.4),
  r1_2wH_freq = c(100, 100),
  check.names = FALSE
)

out <- atom_relax_columns_to_spec_den_term_array(atom_relax_df, "r1")
out$value
dim(out$spec_den_term_array)
dimnames(out$spec_den_term_array)[[2]]

}
