% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ke_prep.R
\name{make_spec_den_relax_data}
\alias{make_spec_den_relax_data}
\title{Build spectral-density relaxation data from expanded atom-pair input}
\usage{
make_spec_den_relax_data(
  atom_relax_data,
  base_rate_mat,
  base_rates,
  perm_rates = c(NA_real_, NA_real_)
)
}
\arguments{
\item{atom_relax_data}{heterogeneous list with at least \code{atom_pairs},
\code{unit}, and \code{relax_data_list}}

\item{base_rate_mat}{transition-rate matrix for ensemble-member exchange}

\item{base_rates}{named numeric vector of symbolic base-process rates used to
label \code{lambda_int_coef}}

\item{perm_rates}{length-two numeric vector describing permutation processes
on the \code{a1} and \code{a2} sides. \code{NA} indicates no permutation, while a
non-missing named value supplies the symbolic rate constant for the
inferred permutation on that side.}
}
\value{
A list with elements \code{atom_pairs}, \code{unit}, \code{relax_data_list},
\code{groupings}, \code{a_int_coef}, \code{lambda_int_coef}, and \code{inferred_multiplicity}
}
\description{
This function constructs the matrices that
implement a user-supplied kinetic model for a given number of ensemble
members. It converts expanded atom-pair input, together with the
corresponding \code{relax_data_list}, into the heterogeneous data structure
needed for spectral-density relaxation calculations.
}
\details{
The function infers the permutation block layout, if any, from the atom-pair
row ordering, validates that all blocks follow the same Cartesian-product
structure, and constructs the internal-motion grouping and coefficient
matrices from \code{base_rate_mat} and \code{perm_rates}. The spectral-density term
arrays in \code{relax_data_list} are passed through unchanged.

The resulting object is intended to work with any rate definition encoded in
\code{relax_data_list}, including term arrays generated by
\code{\link[=make_sigma_spec_den_term_array]{make_sigma_spec_den_term_array()}}, \code{\link[=make_r1_spec_den_term_array]{make_r1_spec_den_term_array()}},
\code{\link[=make_r2_spec_den_term_array]{make_r2_spec_den_term_array()}}, or other functions that produce the same
\code{spec_den_term_array} structure.

The current implementation assumes that the rows in \code{atom_pairs} follow the
expanded ordering convention where rows belonging to the same
relaxation-rate are separated by \eqn{N}, the number of relaxation-rates.
Reordering rows breaks this interpretation and causes validation to fail.
}
